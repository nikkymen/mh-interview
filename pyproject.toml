[project]
name = "mh-interview"
version = "0.1.0"
description = "Aggregate features from time series"
readme = "README.md"
requires-python = "==3.9.5"
dependencies = [
    "numpy<2",
    "pyarrow>=21.0.0",
    "seaborn==0.13.2",
    "setuptools==80.9.0",
    "tables>=3.9.2",
    "tsfresh==0.21.0",
]
